{
  "name": "music",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "check": "node --check app.js && for file in modules/*.js scripts/*.js; do node --check \"$file\"; done && node scripts/validate-quiz-partials.js && node scripts/validate-project-structure.js && node scripts/test-quiz-partial-loader.js && node scripts/test-answer-reveal.js",
    "dev": "http-server -p 8080 -o",
    "test": "npm run check",
    "lint": "eslint ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.50.0",
    "eslint-plugin-html": "^7.1.0",
    "http-server": "^14.1.1",
    "prettier": "^2.8.8"
  },
  "type": "module"
}
